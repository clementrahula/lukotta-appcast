# researcher, in this project

<!-- covers: none -->

The role itself is shared: `~/.claude/agents/researcher.md`, from the workflow repository.
This file is the part that is only true here, and it wins where the two disagree.

What is shipped is an update feed read by every installed copy of a macOS application, on two channels.

Research goes at the repository root.

What is worth establishing from primary sources is Sparkle's appcast schema and its signature requirements at the version the application actually embeds - a feed valid for one Sparkle version is not necessarily valid for another, and the failure is silent.
